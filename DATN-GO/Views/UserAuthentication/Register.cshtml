@model DATN_GO.ViewModels.Authentication.RegisterIdentifierRequest
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Nhập & Đăng Ký</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/homestyle.css" />

</head>
<body>
    <div class="container-fluid vh-100">
        <div class="row h-100">
            <!-- Phần ảnh -->
            <div class="col-md-6 d-none d-md-block p-0">
                <div class="h-100 bg-crimson d-flex align-items-center justify-content-center position-relative">
                    <img src="~/images/imglog.png" class="w-100 h-100" style="object-fit: cover;">
                    <div class="position-absolute top-0 start-0 w-100 h-100 bg-dark bg-opacity-50 d-flex align-items-center justify-content-center">
                        <div class="text-center text-white">
                            <h1 class="display-4 fw-bold mb-3">Chào mừng bạn!</h1>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Phần form -->
            <div class="col-md-6 d-flex align-items-center justify-content-center">
                <div class="w-100">


                    <!-- Form Đăng Ký -->
                    <div id="registerForm" class="p-5">
                        <div>
                            <h2 class="text-center mb-4">Đăng Ký</h2>
                            <form asp-controller="UserAuthentication" asp-action="Register" method="post">
                                <div class="mb-3">
                                    <label asp-for="Identifier" class="form-label">Tài khoản</label>
                                    <input asp-for="Identifier" class="form-control form-control-lg" placeholder="Email / Số điện thoại" />
                                    <span asp-validation-for="Identifier" class="text-danger"></span>
                                </div>
                                <button type="submit" class="btn btn-crimson btn-lg w-100 mb-3">
                                    Đăng Ký
                                </button>
                            </form>

                            <hr class="my-4">

                            <div class="text-center mt-4">
                                <span class="text-muted">Đã có tài khoản? </span>
                                <a asp-action="Login" asp-controller="UserAuthentication" class="text-decoration-none fw-bold">Đăng nhập ngay</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
